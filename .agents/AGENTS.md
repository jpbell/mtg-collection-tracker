# Project-Scoped Rules & Guidelines

- **GitHub Versioning Rule**: Whenever you make code modifications or add features to the codebase, always remember to document the changes in a new release note version in the local database and run the `create_release.py` script to generate a corresponding Git tag and publish the release on GitHub.
